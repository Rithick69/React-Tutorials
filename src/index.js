import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

const rootElement = document.getElementById("root");

// Simple Calculator using import and export

ReactDOM.render(<App />, rootElement);
